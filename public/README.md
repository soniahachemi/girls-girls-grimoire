# public/

Seul dossier réellement déployé par Hostinger (voir `agents/guidelines.md`, section Déploiement, et GGG-5). N'y mettre que les fichiers du site lui-même (HTML/CSS/assets) — jamais d'outillage de dev (`package.json`, `tests/` restent à la racine du dépôt).

À créer par le Developer sur GGG-4 : `index.html` + `assets/style.css`.
